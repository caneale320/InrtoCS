# Caleb Neale, can4ku

age = 0

while age <= 14:
    age = float(input("What is your age? "))

maxAge = (age*2)-13
minAge = (age/2)+7

print("You can date people between", minAge, "and", maxAge, "years old")

