#get all scores in order
#if the score <0 or > max, back to previous step
#if any score is not entered, assign that score 0
#store scores in an array, named scores
#create a percentage array, percentage scores
#check for lowest percentage scores
#ask if drop lowest
#   if yes, remove lowest from percentage array
#   if no do nothing
# display the averge eof the scores array
